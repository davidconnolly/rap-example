# ToDo - Should be able to return a JSON object but at the moment Ember throws a
# 'ember Error: Assertion Failed: Error: Assertion Failed: calling set on
# destroyed object' error.  Although the same doesn't happen for vehicles,
# likely due to the hasMany relationship customers have with vehicles.
""
