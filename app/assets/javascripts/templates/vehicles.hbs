<div class="page-header">
  <h1>Vehicles</h1>
</div>

{{#each}}
  <div class="row">
    <h3>
      <i class="fa fa-truck"></i>
      {{year}} {{make}} belonging to {{customer.name}}
    </h3>
  </div>
{{/each}}
